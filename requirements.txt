fastapi==0.135.1
starlette>=1.0.1,<2   # Starlette 1.x (migrazione su main, build 75). Chiude CVE-2026-48710
uvicorn[standard]>=0.30
jinja2==3.1.6
sse-starlette>=2.0
fastmcp>=3.4,<4   # MCP server (retrieval-only). Con starlette 1.x ok 3.4.2. Pulls mcp, authlib, cryptography, pyjwt
python-multipart>=0.0.9
openai>=1.40
itsdangerous>=2.2
httpx>=0.27
beautifulsoup4>=4.12
pydantic-settings>=2.4
resend>=2.0
markdown>=3.6
# Semantic search runtime (model2vec is numpy-only — NO torch in the image)
model2vec>=0.3
numpy>=1.26
snowballstemmer>=2.2
# Build-only (scripts/build_index.py, scripts/distill_model.py).
# Distillation also needs:  pip install "model2vec[distill]"  (torch/transformers)
# — run locally pre-push only, NEVER installed on Railway.
PyMuPDF>=1.24
Pillow>=10.0
